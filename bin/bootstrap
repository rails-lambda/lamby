#!/usr/bin/env bash

set -e

cd "$(dirname "$0")/.."
BREWFILE=${CI_BREWFILE:-Brewfile}

if [ -f $BREWFILE ]; then
  brew bundle check || {
    echo "==> Installing Homebrew dependencies..."
    brew bundle --file="$BREWFILE" --verbose
  }
fi

brew bootstrap-pyenv-python
brew bootstrap-rbenv-ruby
