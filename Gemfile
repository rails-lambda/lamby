source "https://rubygems.org"
git_source(:github) {|repo_name| "https://github.com/#{repo_name}" }
gemspec

gem 'rails', '~> 8.1'
gem 'mocha', '~> 3.1'
gem 'rack', "~> 3.2"

group :test do
  gem 'lambdakiq'
end
